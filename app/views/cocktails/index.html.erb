<div class="mt-2 bg-white">
  <h1 class="text-center text-decoration-none">Here should be a list of cocktails</h1>
  <% @cocktails.each do |cocktail| %>
  <ul>
    <li>
      <%= link_to cocktail.name, cocktail_path(cocktail) %>
      <%= link_to "🗑", cocktail, method: :delete %>
    </li>
  </ul>
  <% end %>
  <%= link_to "[+]", new_cocktail_path %>
</div>
